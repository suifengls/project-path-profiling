#define read(x)         scanf("%d", &(x))
#define print(x)        printf("%d\n", (x))

int main()
{
    int n,m;
     read(n);
    do {
       n=n-1;
    } while (n>0);
   
    read(m);
    while (m>0) {
      m=m-1;
    }
}
